version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: tasklog
    main: ./main.go
    binary: "tasklog"
    no_unique_dist_dir: false
    env:
      - CGO_ENABLED=1
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}
      - -X main.builtBy=goreleaser

snapshot:
  # Templates: allowed.
  version_template: "{{ .ShortCommit }}-devel"

archives:
  - id: tasklog
    formats: ["tar.gz", "zip"]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  use: github
  sort: asc
  abbrev: 0 
  groups:
    - title: 🚀 Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: 🐛 Bug Fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: 📚 Documentation
      regexp: '^.*?docs(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: 🔧 Improvements
      regexp: '^.*?(refactor|perf|style)(\([[:word:]]+\))??!?:.+$'
      order: 3
    - title: 🧪 Testing
      regexp: '^.*?test(\([[:word:]]+\))??!?:.+$'
      order: 4
    - title: 🏗️ Build & CI
      regexp: '^.*?(build|ci)(\([[:word:]]+\))??!?:.+$'
      order: 5
    - title: 🔒 Security
      regexp: '^.*?sec(\([[:word:]]+\))??!?:.+$'
      order: 6
    - title: 📦 Dependencies
      regexp: '^.*?(deps|chore\(deps\))(\([[:word:]]+\))??!?:.+$'
      order: 7
    - title: 🔄 Other Changes
      order: 999

  filters:
    exclude:
      - '^chore:'
      - '^chore\(deps\):'
      - '^test:'
      - '^docs\(readme\):'
      - 'Merge pull request'
      - 'Merge remote-tracking branch'
      - 'Merge branch'
      - '^WIP:'
      - '^\[skip ci\]'
      - '^\[ci skip\]'

dockers:
  - ids:
      - tasklog
    image_templates:
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER | tolower }}/{{ .ProjectName | tolower }}:{{ .Version }}"
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER | tolower }}/{{ .ProjectName | tolower }}:latest"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--platform=linux/amd64"
    goos: linux
    goarch: amd64
    # Only tag as 'latest' for stable releases (not pre-releases)
    skip_push: false

release:
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## Release {{ .Tag }} ({{ .Date }})

    Welcome to this new release of tasklog!
  footer: |
    ## Installation

    ### Binary
    Download the appropriate binary for your platform from the assets below.

    ### Docker
    ```bash
    docker pull ghcr.io/binsabbar/tasklog:{{ .Tag }}
    ```

    ### Homebrew (coming soon)
    ```bash
    brew install binsabbar/tap/tasklog
    ```

    ## Full Changelog
    See [CHANGELOG.md](https://github.com/binsabbar/tasklog/blob/main/CHANGELOG.md) for details.
  extra_files:
    - glob: dist/tasklog_linux_amd64*/tasklog
      name_template: "tasklog_{{ .Version }}_linux_amd64"
    - glob: dist/tasklog_linux_arm64*/tasklog
      name_template: "tasklog_{{ .Version }}_linux_arm64"
    - glob: dist/tasklog_darwin_amd64*/tasklog
      name_template: "tasklog_{{ .Version }}_darwin_amd64"
    - glob: dist/tasklog_darwin_arm64*/tasklog
      name_template: "tasklog_{{ .Version }}_darwin_arm64"
    - glob: CHANGELOG.md
    - glob: README.md
    - glob: LICENSE
